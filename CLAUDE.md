# CLAUDE.md

이 문서는 이 프로젝트에서 작업할 때 항상 지켜야 하는 공통 규칙을 담는다. 특정 기능(예: 뉴스)에만 해당하는 세부 규칙은 여기 두지 않는다.

## 🚨 최우선 보안 규칙

**네이버 API 키(Client ID/Secret)를 프론트엔드 코드에 절대 넣지 않는다.**

- HTML, CSS, 클라이언트 JS 파일, 클라이언트 번들, 소스맵 등 브라우저에서 확인 가능한 어떤 경로로도 노출되어서는 안 된다.
- 네이버 API 호출은 반드시 서버 사이드 프록시(Edge Function 등)를 통해서만 이루어지며, 클라이언트는 그 프록시를 호출할 뿐 API 키를 직접 다루지 않는다.
- 키는 서버 사이드 환경 변수에만 보관한다.
- 커밋/푸시 전 변경 파일에 API 키나 비밀 값이 포함되어 있지 않은지 항상 확인한다.

## 실패 허용 원칙

**부가 기능이 실패해도 알람 소리 자체는 반드시 울려야 한다.**

- 뉴스 브리핑을 포함해 알람 이외의 부가 기능(외부 API 호출, 렌더링, 음성 합성 등)에서 발생하는 오류는 알람의 핵심 동작(정해진 시각에 소리로 알리는 것)에 절대 영향을 주어서는 안 된다.
- 부가 기능은 알람 로직과 독립적으로 실패할 수 있어야 하며, 부가 기능의 예외가 알람 트리거를 막거나 지연시키는 코드를 작성하지 않는다.

## 아키텍처 원칙

- 외부 API로부터 가져오는 데이터는 DB에 저장하지 않고 매 요청마다 실시간으로 조회한다. 캐싱 테이블이나 주기적 배치/스케줄링 갱신 방식을 임의로 도입하지 않는다.
- 서버 프록시와 클라이언트 간 통신은 HTTPS로만 이루어진다.

## 작업 순서 원칙

새 기능을 구현할 때는 다음 순서를 지킨다.

1. 먼저 가짜(mock) 데이터로 화면(UI)을 완성한다.
2. 그 다음 실제 백엔드/외부 API를 연결한다.
3. 예외 처리(에러 핸들링, 실패 시 UI 등)는 가장 마지막에 추가한다.

## 미확정 사항에 대한 원칙

- 뉴스 검색 키워드/카테고리 방식, 뉴스 브리핑 노출(재생) 시점 등은 아직 확정되지 않았다. **임의로 결정하지 말고**, 결정이 필요한 시점에 사용자에게 먼저 확인한다.

---

뉴스 기능의 세부 규칙(파일 구조, 함수 이름 규칙)은 news/CLAUDE.md를 참고할 것.
