# 최신 뉴스 검색 + 낭독 기능 기술 명세서

## 개요
알람 웹앱에 "최신 뉴스 5개 검색 및 표시 + 음성 낭독" 기능을 추가하기 위한 기술 명세.
기술 스택: **Supabase(백엔드) + React(프론트엔드)**

---

## 전체 구조

```
[React 앱]
   ↓ Supabase 클라이언트 (anon key)
[Supabase]
   ├─ Edge Function (news-fetcher) — 외부 뉴스 API 호출, 서버사이드 프록시
   ├─ Postgres (news_cache 테이블) — 뉴스 캐싱
   └─ pg_cron / Scheduled Function — 주기적 자동 갱신
```

---

## 역할 분담

| 구성 요소 | 역할 |
|---|---|
| **Supabase Edge Function** | 뉴스 API(네이버 뉴스 API, NewsAPI 등)를 서버 사이드에서 호출하는 프록시. API 키를 클라이언트에 노출하지 않고, CORS 제약 없이 뉴스 소스 선택 가능 |
| **Supabase Database (Postgres)** | 가져온 뉴스를 캐싱 → 매번 외부 API를 호출하지 않고 일정 주기(예: 30분)로만 갱신, 무료 티어 호출 제한 절약 |
| **Supabase Cron (pg_cron / Scheduled Function)** | 정해진 주기로 뉴스를 자동 갱신해 DB에 저장 |
| **React (프론트엔드)** | Supabase에서 뉴스 5개를 조회해 카드 형태로 표시 |
| **Web Speech API (SpeechSynthesis)** | 브라우저 내장 TTS로 뉴스 낭독. 별도 키/서버 불필요, 완전 무료, `lang='ko-KR'`로 한국어 지원 |

---

## 데이터 구조 (개념 설계)

**news_cache 테이블**
- id: 고유 식별자
- title: 뉴스 제목
- summary: 뉴스 요약
- source: 출처
- url: 원문 링크
- published_at: 발행 시각
- fetched_at: 수집 시각

읽기 권한은 공개(RLS 정책으로 SELECT만 허용), 쓰기는 Edge Function(서비스 롤 키)만 가능하도록 설계.

---

## 필요한 작업 순서

1. Supabase 프로젝트 생성 및 뉴스 저장용 테이블 설계
2. 외부 뉴스 API(네이버 뉴스 등) 키를 발급받아 Edge Function 환경 변수에 등록 (클라이언트 비노출)
3. Edge Function이 주기적으로 뉴스를 가져와 테이블에 저장하도록 스케줄링
4. React 앱에서 Supabase 클라이언트로 테이블 조회, 상위 5개를 화면에 표시
5. 각 뉴스 카드에 "🔊 듣기" 버튼을 달아 Web Speech API로 낭독 실행

---

## 보안/운영 고려사항

- 뉴스 API 키는 Edge Function 환경 변수에만 저장 (클라이언트 코드에 노출되지 않음)
- React에서 사용하는 Supabase anon key는 RLS 정책으로 보호되므로 노출되어도 안전
- 외부 API 호출 빈도를 캐싱으로 최소화하여 무료 티어 한도 내에서 운영 가능

---

## 참고: TTS 파트는 Supabase와 무관

Web Speech API는 브라우저 내장 기능이므로 Supabase 구조와 별도로 클라이언트단에서 그대로 동작. 추후 더 자연스러운 음성이 필요할 경우 네이버 클로바 보이스, Google Cloud TTS 등으로 대체 가능하나, 이 경우 별도 API 키와 서버 프록시(Edge Function)가 추가로 필요함.
